import React from 'react';
import { render } from '@testing-library/react';
import App from './App';
import {useEventsState} from "./hooks/data-state";

test('sorts events into descending order', () => {
  // useEventsState()
});
